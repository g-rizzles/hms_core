async function generateHelpPanel(data)
{
    let helpPanel = document.createElement('div');
    helpPanel.id = 'help-panel';
    helpPanel.classList.add('help-col');
    // \todo
    return helpPanel;
}